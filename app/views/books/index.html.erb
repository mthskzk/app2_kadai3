<p id="notice">
  <%= flash[:notice] %>
</p>

<h1>Books</h1>
<% @books.each do |book| %>
<div>
  <%= image_tag book.get_image %>
  <p>title :
    <%= link_to book_path(book.id) do %>
      <%= book.title %>
    <% end %>
  </p>
  <p>boey : <%= book.body %></p>
</div>
<% end %>